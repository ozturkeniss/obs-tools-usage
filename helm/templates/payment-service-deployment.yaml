{{- if .Values.services.payment.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "obs-tools-usage.fullname" . }}-payment-service
  labels:
    {{- include "obs-tools-usage.serviceLabels" (dict "serviceName" "payment-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.services.payment.replicaCount }}
  selector:
    matchLabels:
      {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "payment-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "payment-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/payment-service-configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.security.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "obs-tools-usage.serviceAccountName" . }}
      containers:
        - name: payment-service
          {{- with .Values.security.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.services.payment.image.repository }}:{{ .Values.services.payment.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.services.payment.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8082
              protocol: TCP
            - name: grpc
              containerPort: 50052
              protocol: TCP
          env:
            {{- include "obs-tools-usage.mariadbEnv" . | nindent 12 }}
            - name: BASKET_SERVICE_URL
              value: {{ include "obs-tools-usage.basketServiceUrl" . }}
            - name: PRODUCT_SERVICE_URL
              value: {{ include "obs-tools-usage.productServiceUrl" . }}
            {{- range $key, $value := .Values.services.payment.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.services.payment.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.services.payment.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.services.payment.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: var-run
              mountPath: /var/run
      volumes:
        - name: tmp
          emptyDir: {}
        - name: var-run
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
