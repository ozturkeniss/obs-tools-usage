{{- if .Values.services.product.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "obs-tools-usage.fullname" . }}-product-service
  labels:
    {{- include "obs-tools-usage.serviceLabels" (dict "serviceName" "product-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  type: {{ .Values.services.product.service.type }}
  ports:
    - port: {{ .Values.services.product.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.services.product.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "product-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
{{- end }}
