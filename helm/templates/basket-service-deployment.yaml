{{- if .Values.services.basket.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "obs-tools-usage.fullname" . }}-basket-service
  labels:
    {{- include "obs-tools-usage.serviceLabels" (dict "serviceName" "basket-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.services.basket.replicaCount }}
  selector:
    matchLabels:
      {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "basket-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "basket-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/basket-service-configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.security.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "obs-tools-usage.serviceAccountName" . }}
      containers:
        - name: basket-service
          {{- with .Values.security.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.services.basket.image.repository }}:{{ .Values.services.basket.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.services.basket.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
            - name: grpc
              containerPort: 50051
              protocol: TCP
          env:
            {{- include "obs-tools-usage.redisEnv" . | nindent 12 }}
            - name: PRODUCT_SERVICE_URL
              value: {{ include "obs-tools-usage.productServiceUrl" . }}
            {{- range $key, $value := .Values.services.basket.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.services.basket.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.services.basket.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.services.basket.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: var-run
              mountPath: /var/run
      volumes:
        - name: tmp
          emptyDir: {}
        - name: var-run
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
