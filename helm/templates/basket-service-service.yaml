{{- if .Values.services.basket.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "obs-tools-usage.fullname" . }}-basket-service
  labels:
    {{- include "obs-tools-usage.serviceLabels" (dict "serviceName" "basket-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  type: {{ .Values.services.basket.service.type }}
  ports:
    - port: {{ .Values.services.basket.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.services.basket.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "basket-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
{{- end }}
