{{- if .Values.services.product.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "obs-tools-usage.fullname" . }}-product-service
  labels:
    {{- include "obs-tools-usage.serviceLabels" (dict "serviceName" "product-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.services.product.replicaCount }}
  selector:
    matchLabels:
      {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "product-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "obs-tools-usage.serviceSelectorLabels" (dict "serviceName" "product-service" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/product-service-configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.security.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "obs-tools-usage.serviceAccountName" . }}
      containers:
        - name: product-service
          {{- with .Values.security.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.services.product.image.repository }}:{{ .Values.services.product.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.services.product.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: grpc
              containerPort: 50050
              protocol: TCP
          env:
            {{- include "obs-tools-usage.postgresEnv" . | nindent 12 }}
            {{- range $key, $value := .Values.services.product.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.services.product.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.services.product.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.services.product.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: var-run
              mountPath: /var/run
      volumes:
        - name: tmp
          emptyDir: {}
        - name: var-run
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
